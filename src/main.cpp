#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include "feature.h"

using namespace std;

int main(int argc, char *argv[])
{
    if(argc <=1 ){
        cout << "Please specify data directory!\n"; 
        exit(1);
    }
    
    string directory(argv[1]);

    

}
